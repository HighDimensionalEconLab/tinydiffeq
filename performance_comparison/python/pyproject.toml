[project]
name = "python"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "diffrax>=0.7.2",
    "jax[cuda13]>=0.10.2 ; sys_platform == 'linux'",
    "tinydiffeq",
]

[tool.uv.sources]
tinydiffeq = { path = "../../", editable = true }
